# Bld website

This repository contains the source for the `Bld website`. The website is built with Astro.

## Building the project

Execute an `npm install` to install all required dependencies and then use `npm run build`.

Use the `npm run dev` command to run the dev server.
